<%= simple_form_for(@user, url: seller_path(@current_user)) do |f| %>
  <%= f.input :name, label: "Nome"%>
  <%= f.input :bio, label: "Apresentação" %>
  <%= f.input :photo, label: "Foto" %>
  <%= f.button :submit, "Atualizar", class: 'btn general' %>
<% end %>
