<%= render 'shared/new_navbar' %>
<div class="container mt-5">
  <h3 class="text-center pt-2 mb-2 mt-2">Lista Atual de Produtos</h3>
    <% @inventory.products.each do |product| %>
      <% if product.id != @product.id %>
        <h5><%= product.name %> - R$<%= product.price.to_s.gsub(".", ",") + 0.to_s %></h5>
      <% end %>
    <% end %>
  <%= simple_form_for(@product) do |f| %>
    <%= f.input :name, label: "Nome" %>
    <%= f.input :price, label: "Preço" %>
    <%= f.button :submit,"Atualizar Produto", class: 'btn general' %>
  <% end %>
</div>

