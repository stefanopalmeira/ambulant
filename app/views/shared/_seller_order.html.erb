<%= simple_form_for(order, url: seller_order_path(order), html: { class: 'd-inline-block' }, remote: true) do |f| %>
    <%= hidden_field_tag 'accepted', 2 %>
    <%= f.submit "Aceitar", class: 'btn aceito' %>
  <% end %>
  <%= simple_form_for(order, url: seller_order_path(order), html: { class: 'd-inline-block' }, remote: true) do |f| %>
    <%= hidden_field_tag 'accepted', 3 %>
    <%= f.submit "Negar", class: 'btn negado' %>
  <% end %>
