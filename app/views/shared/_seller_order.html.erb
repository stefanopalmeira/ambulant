<%= simple_form_for(order, url: seller_order_path(order), html: { class: 'd-inline-block' }) do |f| %>
    <%= hidden_field_tag 'accepted', 2 %>
    <%= f.submit "Aceito", class: 'btn btn-success' %>
  <% end %>
  <%= simple_form_for(order, url: seller_order_path(order), html: { class: 'd-inline-block' }) do |f| %>
    <%= hidden_field_tag 'accepted', 3 %>
    <%= f.submit "Negado", class: 'btn btn-danger' %>
  <% end %>